Вы собираетесь создать программу для шифрования и дешифрования с помощью [шифра Цезаря](https://en.wikipedia.org/wiki/Caesar_cipher).

### Демо
https://appbrewery.github.io/python-day8-demo/

### ЗАДАНИЕ-1: 
Создайте функцию под названием `encrypt()`, которая принимает `original_text` и `shift_amount` в качестве 2 входных параметров.

### ЗАДАНИЕ-2: 
Внутри функции 'encrypt' сдвигайте каждую букву `original_text` в перед в алфавите на величину `shift_amount` и выведите зашифрованный текст.

Вы можете использовать встроенную функцию Python `index()`, чтобы узнать позицию элемента в списке. например:
```
fruits = ["Apple", "Pear", "Orange"]
fruits.index("Pear") #1
```

например, если у нас есть следующие значения:
```
plain_text = "hello"
shift_amount = 1
```
Итоговый зашифрованный результат должен быть:

`Вот зашифрованный результат: ifmmp`

Где каждая буква 'hello' сдвигается на 1. 

<div class="hint">
Давайте разобьем проблему:

  1. Вам нужен цикл for для циклического просмотра каждой буквы в plain_text. 
  2. Найдите позицию каждой буквы в списке алфавита.
3. Добавьте желаемое пользователем значение shift_amount к позиции, чтобы получить позицию зашифрованной буквы.
4. Найдите соответствующую букву для этой позиции.
5. Добавьте каждую закодированную букву в новую строку и распечатайте ее после окончания цикла.

</div>


### ЗАДАНИЕ-3: 
Вызовите функцию `encrypt()` и передайте в нее ввод пользователя. Вы должны иметь возможность проверить код и зашифровать сообщение.


### ЗАДАНИЕ-4: 
Что происходит, если вы пытаетесь сдвинуть букву 'z' вперед на 9? Сможете ли вы исправить код?

<div class="hint">
  Существует много способов решить эту задачу.
1. Вы можете добавить более 1 набора алфавита в список букв алфавита.
2. Вы можете сдвинуть shift_amount так, чтобы он всегда находился в пределах 0 - 25 и вписывался в список.
3. Вы можете использовать модуль, чтобы получить остаток.
</div>