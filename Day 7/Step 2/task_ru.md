### TODO-1
- Создайте пустую строку под названием `placeholder`.
- Для каждой буквы в chosen_word добавьте `_` в `placeholder`.
- Так, если `chosen_word` было "apple", `placeholder` должно быть `_ _ _ _ _` с 5 `"_"`, представляющими каждую букву для угадывания.
- Выведите на печать `hint`.

<div class="hint">
  Помните, что вы можете использовать функцию range() внутри цикла для выполнения действия определенное количество раз.
</div>


### TODO-2
- Создайте пустую строку под названием "display".
- Пройдите через каждую букву в `chosen_word`
- Если буква на этой позиции совпадает с `guess`, то раскройте эту букву в `display` на этой позиции.
- к примеру Если пользователь угадал "p", а выбранное слово было "apple", то `display` должно быть `_ p p _ _`.
- Выведите `display`, и вы должны увидеть угаданную букву в правильной позиции.
- Но каждая буква, которая не совпадает, представлена с "_".

<div class="hint">
  Помните, что цикл for пройдет через каждую букву chosen_word по порядку. Вы можете использовать это, чтобы создать новую строку, добавляя букву или "_".
</div>