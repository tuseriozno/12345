### Выберите уровень сложности
- **Нормальный** 😎: Используйте все подсказки ниже, чтобы выполнить проект.
- **Сложный** 🤔: Используйте только подсказки 1, 2, 3, чтобы выполнить проект.
- **Очень сложный** 😭: Используйте только подсказки 1 и 2, чтобы выполнить проект.
- **Эксперт** 🤯: Используйте только подсказку 1, чтобы выполнить проект.

### Наши правила Blackjack

- Колода неограниченного размера.
- Джокеров нет.
- Валет/Дама/Король считаются за 10.
- Туз может считаться 11 или 1.
- Используйте следующий список как колоду карт:

`cards = [11, 2, 3, 4, 5, 6, 7, 8, 9, 10, 10, 10, 10]`
- Карты в списке имеют равную вероятность выпадения.
- Карты не удаляются из колоды по мере того, как они снимаются.
- Станьте на место дилера - это компьютер.


<div class="hint" title="Подсказка 1">
Попробуйте играть в Blackjack на этом сайте:

https://games.washingtonpost.com/games/blackjack/

А затем попробуйте наш готовый проект Blackjack здесь:

https://appbrewery.github.io/python-day11-demo/
</div>

<div class="hint" title="Подсказка 2">
Прочитайте обзор требований к программе:

http://listmoz.com/view/6h34DJpvJBFVRlZfJvxF

Затем попробуйте создать свою собственную блок-схему для программы.
</div>

<div class="hint" title="Подсказка 3">
Скачайте и прочитайте эту блок-схему, которую я создал:

https://drive.google.com/uc?export=download&id=1rDkiHCrhaf9eX7u7yjM1qwSuyEk-rPnt

</div>


<div class="hint" title="Подсказка 4">
Создайте функцию <code>deal_card()</code>, которая будет использовать список ниже, чтобы возвращать случайную карту.

<code>cards = [11, 2, 3, 4, 5, 6, 7, 8, 9, 10, 10, 10, 10]</code>

Помните, что Туз - это 11.
</div>

<div class="hint" title="Подсказка 5">
Раздайте игроку и компьютеру по 2 карты каждому, используя <code>deal_card()</code> и <code>append()</code>.

<code>user_cards = []</code>

<code>computer_cards = []</code>
</div>

<div class="hint" title="Подсказка 6">
Создайте функцию под названием <code>calculate_score()</code>, которая принимает в качестве входного параметра список карт и возвращает сумму всех карт в списке как счет. Для реализации вам может помочь функция <code>sum()</code>.
</div>


<div class="hint" title="Подсказка 7">
Внутри <code>calculate_score()</code> проверьте, есть ли блэкджек (рука только с 2 картами: туз + 10), и вместо реального счета верните <code>0</code>. <code>0</code> будет представлять блэкджек в нашей игре.
</div>


<div class="hint" title="Подсказка 8">
Внутри <code>calculate_score()</code> проверьте наличие 11 (туза). Если счет превышает 21, замените 11 на 1. Вам может потребоваться поискать в Google документацию о встроенных функциях Python <code>append()</code> и <code>remove()</code>.

https://developers.google.com/edu/python/lists#list-methods
</div>

<div class="hint" title="Подсказка 9">
Вызвать функцию <code>calculate_score()</code>. Если у компьютера или пользователя есть блэкджек (0), или если счет пользователя превышает 21, то игра заканчивается.
</div>

<div class="hint" title="Подсказка 10">
Если игра не закончилась, спросите у пользователя, хочет ли он взять еще одну карту. Если да, то используйте функцию <code>deal_card()</code>, чтобы добавить еще одну карту в список <code>user_cards</code>. Если нет, то игра закончена.
</div>

<div class="hint" title="Подсказка 11">
С каждой новой картой счет нужно проверять заново, а проверки из подсказки 9 нужно повторять, пока игра не закончится.
</div>

<div class="hint" title="Подсказка 12">
Когда пользователь закончит, пора дать играть компьютеру. Компьютер должен продолжать брать карты до тех пор, пока его счет не станет меньше 17.
</div>

<div class="hint" title="Подсказка 13">
Создания функцию с названием <code>compare()</code> и передайте в нее <code>user_score</code> и <code>computer_score</code>. 

- Если у компьютера и пользователя одинаковый счёт, то это ничья.
- Если у компьютера есть блэкджек (0), то пользователь проигрывает. 
- Если у пользователя есть блэкджек (0), то пользователь выигрывает. 
- Если <code>user_score</code> больше 21, то пользователь проигрывает. 
- Если <code>computer_score</code> больше 21, то компьютер проигрывает. 
- Если ничего из вышеперечисленного не произошло, то выигрывает игрок с наивысшим счетом.
</div>

<div class="hint" title="Подсказка 14">
Спросите у пользователя, хотят ли они перезапустить игру. Если они ответят да, очистите консоль и начните новую игру в блэкджек, показав логотип из art.py.
</div>

